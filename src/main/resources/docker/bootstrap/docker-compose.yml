version: '3.8'

services:
  postgres:
    container_name: db
    image: postgres:16.3
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: course
    ports:
      - "5432:5432"




